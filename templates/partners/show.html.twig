{% extends 'base.html.twig' %}

{% block title %}Partners{% endblock %}

{% block body %}
    <h1>Partners</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner.name }}</td>
            </tr>
            <tr>
                <th>Logo.name</th>
                <td>{{ partner.logo.name }}</td>
            </tr>
            <tr>
                <th>Logo.originalName</th>
                <td>{{ partner.logo.originalName }}</td>
            </tr>
            <tr>
                <th>Logo.mimeType</th>
                <td>{{ partner.logo.mimeType }}</td>
            </tr>
            <tr>
                <th>Logo.size</th>
                <td>{{ partner.logo.size }}</td>
            </tr>
            <tr>
                <th>Logo.dimensions</th>
                <td>{{ partner.logo.dimensions }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partners_index') }}">back to list</a>

    <a href="{{ path('partners_edit', {'id': partner.id}) }}">edit</a>

    {{ include('partners/_delete_form.html.twig') }}
{% endblock %}
