{% extends 'base.html.twig' %}

{% block title %}{{'company.edit'|trans({}, 'company')|title}}{% endblock %}

{% block body %}
<h1>{{'company.edit'|trans({}, 'company')|title}}</h1>

{{ include('company/_form.html.twig', {'button_label': 'Update'}) }}

<a href="{{ path('company_index') }}">{{'general.back'|trans({}, 'general')|capitalize}}</a>

{{ include('company/_delete_form.html.twig') }}
{% endblock %}