<form method="post" action="{{ path('cie_dates_delete', {'id': cie_date.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cie_date.id) }}">
    <button class="btn">Delete</button>
</form>
