{% extends 'base.html.twig' %}

{% block title %}
	{{ 'partner.edit'|trans({}, 'partners')|title }}
{% endblock %}

{% block header %}{% endblock %}
{% block body %}
	<h1>{{ 'partner.edit'|trans({}, 'partners')|title }}</h1>
	<div class="ml-3">
		{{ include('admin/partners/_form.html.twig', {'button_label': 'Update'}) }}

		<a class="badge badge-secondary p-2" href="{{ path('partners_index') }}">{{'general.back'|trans({}, 'general')|capitalize}}</a>

		{{ include('admin/partners/_delete_form.html.twig') }}
	</div>
{% endblock %}
