{% extends 'base.html.twig' %}

{% block title %}
	{{'team.edit'|trans({}, 'team')|title }}
{% endblock %}

{% block header %}{% endblock %}
{% block body %}
	<h1>{{'team.edit'|trans({}, 'team')|title }}</h1>
	<div class="ml-3">

		{{ include('admin/team/_form.html.twig', {'button_label': 'Update'}) }}

		<a class="badge badge-secondary p-2" href="{{ path('team_index') }}">{{'general.back'|trans({}, 'general')|capitalize}}</a>

		{{ include('admin/team/_delete_form.html.twig') }}
	</div>
{% endblock %}
