{% extends 'back_base.html.twig' %}

{% block title %}
	{{'company.edit'|trans({}, 'company')|title}}
{% endblock %}

{% block body %}
	<h1>{{'company.edit'|trans({}, 'company')|title}}</h1>
	<div class="ml-3">
		{{ include('admin/company/_form.html.twig', {'button_label': 'Update'}) }}

		<a class="badge badge-secondary p-2" href="{{ path('company_index') }}">{{'general.back'|trans({}, 'general')|capitalize}}</a>

		{{ include('admin/company/_delete_form.html.twig') }}
	</div>
{% endblock %}
