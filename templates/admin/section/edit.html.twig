{% extends 'back_layout.html.twig' %}

{% block title %}
	{{ 'section.edit'|trans({}, 'section')|title }}
{% endblock %}

{% block body %}
	<h1>{{ 'section.edit'|trans({}, 'section')|title }}</h1>
	<div class="ml-3">

		{{ include('admin/section/_form.html.twig', {'button_label': 'Update'}) }}

		<a class="badge badge-secondary p-2" href="{{ path('page_list_show', {'id': section.belongToPageId}) }}" title="Retour à la liste des articles">{{'general.back'|trans({}, 'general')|capitalize}}</a>

		{{ include('admin/section/_delete_form.html.twig') }}
	</div>

{% endblock %}
