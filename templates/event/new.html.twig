{% extends 'base.html.twig' %}

{% block title %}{{'event.create'|trans({}, 'event')|title}}{% endblock %}

{% block body %}
    <h1>{{'event.create'|trans({}, 'event')|title}}</h1>

    {{ include('event/_form.html.twig') }}

    <a href="{{ path('event_index') }}">{{'general.back'|trans({}, 'general')|capitalize}}</a>
{% endblock %}
