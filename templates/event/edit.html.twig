{% extends 'base.html.twig' %}

{% block title %}{{ 'event.edit'|trans({}, 'event')|title }}{% endblock %}

{% block body %}
<h1>{{ 'event.edit'|trans({}, 'event')|title }}</h1>
<div class="ml-3">

    {{ include('event/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="badge badge-secondary p-2"
        href="{{ path('event_index') }}">{{'general.back'|trans({}, 'general')|capitalize}}</a>

    {{ include('event/_delete_form.html.twig') }}
</div>
{% endblock %}