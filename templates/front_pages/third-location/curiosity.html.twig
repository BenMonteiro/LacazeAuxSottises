{% extends "front_templates/page_template.html.twig" %}

{% block body %}
	<div class="container-fluid">

		{% include('/front_pages/_intro_section_template.html.twig') %}
		<div class="offset-2">
			<h2 class="font-weight-bold ml-4 mb-3">{{ 'general.third_location.all_year'|trans({}, 'general')|capitalize }}</h2>
		</div>
		{% for section in sections %}
			{% if section.name == "all year" %}
				<section class=" d-md-flex align-items-start justify-content-between offset-lg-2 col-lg-8 mb-3">
					<div class="d-flex flex-column align-items-center my-3">
						<figure class="cubic-image-div">
							<img class="image-frame" src="{{ asset('images/cadre.png') }}" alt="cadre noir entourant l'image" aria-hidden="true">
							<img class="cubic-image" src="{{ asset('images/uploads/paragraphImage/' ~ section.image.name) }}" alt="{{section.imageDescription}}">
						</figure>
					</div>
					<article class="d-flex flex-column mt-4 framedArticle red-dot text-md-left text-center">
						{% if section.title %}
							<h2 class="colored mb-0">{{ section.title|raw }}</h2>
						{% endif %}
						{% if section.subTitle %}
							<h3 class="font-weight-bold ml-0 mt-0">{{ section.subTitle|raw }}</h3>
						{% endif %}
						<div>{{ section.content|raw}}</div>
					</article>
				</section>
			{% endif %}
		{% endfor %}
		<div class="offset-2">
			<h2 class="font-weight-bold  ml-4 mb-3">{{ 'general.third_location.punctual'|trans({}, 'general')|capitalize }}</h2>
		</div>
		{% for section in sections %}
			{% if section.name == 'punctual' %}
				<section class=" d-md-flex align-items-start justify-content-between offset-lg-2 col-lg-8 mb-3">
					<div class="d-flex flex-column align-items-center my-3">
						<figure class="cubic-image-div">
							<img class="image-frame" src="{{ asset('images/cadre.png') }}" alt="cadre noir entourant l'image" aria-hidden="true">
							<img class="cubic-image" src="{{ asset('images/uploads/paragraphImage/' ~ section.image.name) }}" alt="{{section.imageDescription}}">
						</figure>
					</div>
					<article class="d-flex flex-column mt-4 framedArticle dash text-md-left text-center">
						{% if section.title %}
							<h2 class="colored">{{ section.title|raw }}</h2>
						{% endif %}
						{% if section.subTitle %}
							<h3 class="font-weight-bold ml-0 mt-0">{{ section.subTitle|raw }}</h3>
						{% endif %}
						<div>{{ section.content|raw}}</div>
					</article>
				</section>
			{% endif %}
		{% endfor %}

		<div class="offset-2">
			<h2 class="font-weight-bold  ml-4 mb-3">{{ 'general.third_location.do_it_yourself'|trans({}, 'general')|capitalize }}</h2>
		</div>
		{% for section in sections %}
			{% if section.name == 'do it' %}
				<section class=" d-md-flex align-items-start justify-content-between offset-lg-2 col-lg-8 mb-3">
					<div class="d-flex flex-column align-items-center my-3">
						<figure class="cubic-image-div">
							<img class="image-frame" src="{{ asset('images/cadre.png') }}" alt="cadre noir entourant l'image" aria-hidden="true">
							<img class="cubic-image" src="{{ asset('images/uploads/paragraphImage/' ~ section.image.name) }}" alt="{{section.imageDescription}}">
						</figure>
					</div>
					<article class="d-flex flex-column mt-4 framedArticle red-dash text-md-left text-center">
						{% if section.title %}
							<h2 class="colored mb-0">{{ section.title|raw }}</h2>
						{% endif %}
						{% if section.subTitle %}
							<h3 class="font-weight-bold ml-0 mt-0">{{ section.subTitle|raw}}</h3>
						{% endif %}
						<div>{{ section.content|raw}}</div>
					</article>
				</section>
			{% endif %}
		{% endfor %}
	</div>
{% endblock %}
