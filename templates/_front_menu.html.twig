<nav class="navbar navbar-expand-md navbar-light">
    <a class="navbar-brand" href=''><img class='logo' src="{{asset('images/logo.jpg')}}"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse nav-tabs nav d-md-flex flex-md-column" id="navbarNavAltMarkup" role="tablist">
        <ul class="row navbar-nav mr-2 ml-md-auto ml-2">
            <li>
                <a class="nav-link" href="{{path('home')}}" role="link"><i class="fas fa-1x fa-home"></i></a>
            </li>
            <li>
                <a class="nav-item nav-link" href="" id="nav-association-tab" data-toggle="tab"
                    data-target="#nav-association" role="tab" aria-controls="nav-association">
                    {{'front.association.label'|trans({}, 'menu')}}
                </a>
            </li>
            <li>
                <a class="nav-item nav-link" href="" id="nav-season-tab" data-toggle="tab" data-target="#nav-season"
                    role="tab" aria-controls="nav-contact" aria-selected="false">
                    {{'front.season'|trans({}, 'menu')|title}}
                </a>
            </li>
            <li>
                <a class="nav-item nav-link" href="" id="nav-fest-tab" data-toggle="tab" data-target="#nav-fest"
                    role="tab" aria-controls="nav-contact" aria-selected="false">
                    {{'front.festival'|trans({}, 'menu')|title}}
                </a>
            </li>
            <li>
                <a class="nav-item nav-link" href="" id="nav-place-tab" data-toggle="tab" data-target="#nav-place"
                    role="tab" aria-controls="nav-contact" aria-selected="false">
                    {{'front.place'|trans({}, 'menu')|title}}
                </a>
            </li>
            <li>
                <a class="nav-item nav-link" href="" id="nav-activity-tab" data-toggle="tab"
                    data-target="#nav-activities" role="tab" aria-controls="nav-contact" aria-selected="false">
                    {{'front.activities'|trans({}, 'menu')|title}}
                </a>
            </li>
        </ul>
        <ul class="row navbar-nav mr-md-5 ml-md-auto ml-4">
            <li>
                {% for page in pages %}
                {% if page.tab == 'locations' %}
                <a class="nav-link"
                    href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
                {% endif %}
                {% endfor %}
            </li>
            <li>
                <a class="nav-item nav-link" href="" id="nav-volunteering-tab" data-toggle="tab"
                    data-target="#nav-volunteering" role="tab" aria-controls="nav-contact" aria-selected="false">
                    {{'front.volunteering'|trans({}, 'menu')|title}}
                </a>
            </li>
            <li>
                {% for page in pages %}
                {% if page.tab == 'partenaires' %}
                <a class="nav-link"
                    href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
                {% endif %}
                {% endfor %}
            </li>
            <li>
                {% for page in pages %}
                {% if page.tab == 'support' %}
                <a class="nav-link"
                    href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
                {% endif %}
                {% endfor %}
            </li>
            <li>
                {% for page in pages %}
                {% if page.tab == 'contact' %}
                <a class="nav-link"
                    href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
                {% endif %}
                {% endfor %}
            </li>
        </ul>
    </div>
</nav>

<div class="tab-content" id="nav-tabContent">
    <div class="tab-pane fade" id="nav-association" role="tabpanel" aria-labelledby="nav-association-tab">
        {% for page in pages %}
        {% if page.tab == 'association' %}
        <a href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
        {% endif %}
        {% endfor %}

    </div>

    <div class="tab-pane fade" id="nav-season" role="tabpanel" aria-labelledby="nav-season-tab">
        {% for page in pages %}
        {% if page.tab == 'saison' %}
        <a href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
        {% endif %}
        {% endfor %}
    </div>

    <div class="tab-pane fade" id="nav-fest" role="tabpanel" aria-labelledby="nav-fest-tab">
        {% for page in pages %}
        {% if page.tab == 'festival' %}
        <a href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
        {% endif %}
        {% endfor %}
    </div>

    <div class="tab-pane fade" id="nav-place" role="tabpanel" aria-labelledby="nav-place-tab">
        {% for page in pages %}
        {% if page.tab == 'tiers-lieu' %}
        <a href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
        {% endif %}
        {% endfor %}
    </div>

    <div class="tab-pane fade" id="nav-activities" role="tabpanel" aria-labelledby="nav-activity-tab">
        {% for page in pages %}
        {% if page.tab == 'activités-du-lieu' %}
        <a href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
        {% endif %}
        {% endfor %}
    </div>

    <div class="tab-pane fade" id="nav-volunteering" role="tabpanel" aria-labelledby="nav-volunteering-tab">
        {% for page in pages %}
        {% if page.tab == 'bénévolat' %}
        <a href="{{ path('page_show', {'pageSlug': page.pageSlug}) }}">{{page.name|trans({}, 'menu')|title}}</a>
        {% endif %}
        {% endfor %}
    </div>
</div>